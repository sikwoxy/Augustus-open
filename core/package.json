{
  "name": "@augustus/core",
  "version": "1.0.0",
  "description": "Augustus runtime — types, interfaces, and factory",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "yalc:push": "pnpm run build && yalc push"
  },
  "devDependencies": {
    "@types/node": "^25.7.0",
    "typescript": "^5.6.0"
  }
}
